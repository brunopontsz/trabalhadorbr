<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="Descrição do seu site para SEO."/>
    <title>Calcule sua rescisão trabalhista - Trabalhador Brasileiro</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, minimum-scale=1"/>
  </head>
  <body>
    <% if @qr_code_image.present? %>
      <img src="data:image/jpeg;base64,<%= @qr_code_image %>" alt="Imagem Carregada">
    <% elsif @message.present? %>
      <p><%= @message %></p>
    <% else %>
      <p>Recarregue a página</p>
    <% end %>
  </body>
<html>
